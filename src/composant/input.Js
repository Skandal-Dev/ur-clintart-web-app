import react from "react";
import reactDOM from "react-dom";
import { useState } from "react";
import { render } from "react-dom";


export default function Input(set,get){
    


    return 
        (
            <input placeholder={set} onChange={this.handleChange(this.props.setNameCard)} type="text"/>
        );
}